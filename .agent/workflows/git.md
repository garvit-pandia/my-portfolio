---
description: Version Control (Git)
---

// turbo-all
1. Run `git status` to see current changes.
2. Run `git add .` to stage all changes.
3. Run `git commit -m "update"` or a provided message to save changes.
4. Run `git push` to upload changes to the repository.
