---
description: Build, commit, and push changes in one step
---

// turbo-all
1. Run `npm run build` to verify no build errors.
2. Run `git add .` to stage all changes.
3. Run `git commit -m "<provided or auto-generated message>"` to commit.
4. Run `git push` to push to remote.
