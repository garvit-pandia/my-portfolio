---
description: Project Maintenance and Diagnostics
---

// turbo-all
1. Run `npm audit fix` for security patches.
2. Run `npm list --depth=0` to see installed packages.
3. Check environment versions with `node -v` and `npm -v`.
